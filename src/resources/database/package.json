{
  "name": "database",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "seed": "npx serverless dynamodb seed",
    "start": "npm run migrate && npm run seed",
    "migrate": "npx serverless dynamodb migrate",
    "build": "serverless package --stage dev --package sls-package-output"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless-dynamodb-local": "^0.2.39"
  }
}
