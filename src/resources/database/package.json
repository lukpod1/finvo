{
  "name": "database",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "seed": "sls dynamodb seed",
    "start": "npm run migrate",
    "migrate": "sls dynamodb start --migrate",
    "build": "sls package --stage dev --package sls-package-output"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serverless-dynamodb-local": "^0.2.40"
  }
}
